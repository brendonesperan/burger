<h3>Menu Options</h3>
<div>
  {{#each foods}}
    <div>
      <p><strong>{{food_name}}</strong><button class="eat-burger" id="eat-burger" data-id="{{id}}" style="float: right">Eat burger</button>
      </p>
    </div>
  {{/each}}
</div>
